/**
 * Якоря аннотаций в системе координат фигуры (общие для DOM-оверлея и 3D-сцены):
 *  - bx: смещение от оси тела в долях роста (±0.5 ≈ кончики разведённых рук)
 *  - by: высота как доля роста от стоп (0) до макушки (≈1)
 * `icon` — где висит карточка органа; `node` — точка на теле, куда приходит линия.
 * AnatomyScene проецирует их реальной камерой, AnatomyAnnotations рисует.
 */
export interface AnnoAnchor {
  icon: { bx: number; by: number };
  node: { bx: number; by: number };
}

// Порядок СТРОГО как у карточек в AnatomyAnnotations:
// brain, heart, lungs, kidneys, cell, ecg, dna, lab.
// node — точка на нужном органе, icon — где висит карточка.
export const ANNO_ANCHORS: AnnoAnchor[] = [
  // Мозг → голова, карточка слева от головы (над макушкой прячется под шапку)
  { node: { bx: 0.0, by: 0.92 }, icon: { bx: -0.16, by: 0.99 } },
  // Сердце → грудь, карточка справа сверху
  { node: { bx: 0.05, by: 0.71 }, icon: { bx: 0.3, by: 0.84 } },
  // Лёгкие → грудь, карточка слева сверху
  { node: { bx: -0.07, by: 0.73 }, icon: { bx: -0.3, by: 0.84 } },
  // Почки → поясница, карточка вплотную к талии (правее — текст стадии)
  { node: { bx: 0.055, by: 0.54 }, icon: { bx: 0.17, by: 0.55 } },
  // Клетки/иммунитет → селезёнка/живот, карточка слева посередине
  { node: { bx: -0.04, by: 0.57 }, icon: { bx: -0.34, by: 0.62 } },
  // ЭКГ → область сердца, панель слева снизу
  { node: { bx: -0.02, by: 0.68 }, icon: { bx: -0.3, by: 0.36 } },
  // ДНК → таз/торс, бейдж справа снизу (ниже блока текста стадии)
  { node: { bx: 0.04, by: 0.5 }, icon: { bx: 0.28, by: 0.26 } },
  // Анализы (микроскоп) → низ живота, бейдж слева у ног
  { node: { bx: -0.04, by: 0.47 }, icon: { bx: -0.22, by: 0.18 } },
];

/**
 * Хотспоты — светящиеся точки на суставах/узлах тела (как маркеры на рентгене).
 * Проецируются той же камерой следом за ANNO_ANCHORS (по 2 числа на точку).
 */
export const HOTSPOT_ANCHORS: { bx: number; by: number }[] = [
  { bx: -0.12, by: 0.775 }, // левое плечо
  { bx: 0.12, by: 0.775 }, // правое плечо
  { bx: -0.21, by: 0.615 }, // левый локоть
  { bx: 0.21, by: 0.615 }, // правый локоть
  { bx: -0.065, by: 0.5 }, // левое бедро
  { bx: 0.065, by: 0.5 }, // правое бедро
  { bx: -0.05, by: 0.28 }, // левое колено
  { bx: 0.05, by: 0.28 }, // правое колено
];
